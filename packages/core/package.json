{
	"name": "@public/core",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"test": "sst bind vitest",
		"typecheck": "tsc -noEmit",
		"local:migrate": "DOTENV_CONFIG_PATH=.env.local bun drizzle-kit generate && bun drizzle-kit migrate",
		"migrate": "DOTENV_CONFIG_PATH=.env bun drizzle-kit generate && bun drizzle-kit migrate"
	},
	"devDependencies": {
		"@types/bun": "^1.1.10",
		"@types/node": "^20.14.9",
		"@types/uuid": "^10.0.0",
		"drizzle-kit": "^0.30.1",
		"supabase": "^2.1.1",
		"vitest": "^1.6.0"
	},
	"files": [
		"src/**/*"
	],
	"dependencies": {
		"drizzle-orm": "^0.38.3",
		"drizzle-zod": "^0.6.1",
		"geolib": "^3.3.4",
		"postgres": "^3.4.5",
		"sst": "3.4.32"
	}
}
